{
  "name": "chasket-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Official documentation site for Chasket — built with Chasket",
  "scripts": {
    "build": "chasket build && node scripts/postbuild.js",
    "dev": "chasket build && node scripts/postbuild.js && chasket dev",
    "preview": "chasket build && node scripts/postbuild.js"
  },
  "dependencies": {
    "@chasket/chasket": "^0.2.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/UltraEgoist/chasket.dev.git"
  },
  "homepage": "https://chasket.dev",
  "bugs": {
    "url": "https://github.com/UltraEgoist/chasket.dev/issues"
  },
  "author": "Aspect",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
